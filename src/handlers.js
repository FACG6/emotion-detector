const path = require("path");
const fs = require("fs");

const handleHome = () => {



















};

const handleStatic = () => {
























};

const handleImg = () => {};

const handleNotFoundError = () => {



























};

const handleServerError = () => {


























    
};

module.exports = {
  handleHome,
  handleStatic,
  handleImg,
  handleNotFoundError,
  handleServerError
};
